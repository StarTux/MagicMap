package com.winthier.minimap;

// import java.awt.image.BufferedImage;
// import java.io.File;
// import java.lang.reflect.Field;
// import javax.imageio.ImageIO;
// import org.bukkit.map.MapPalette;
// import org.bukkit.Material;
import org.junit.Test;

public final class MiniMapTest {
    @Test
    public void main() throws Exception {
        // java.io.PrintStream out = new java.io.PrintStream(new File("target/colors.txt"));
        // for (Field field: Blocks.class.getDeclaredFields()) {
        //     if (field.getType().equals(Block.class)) {
        //         Block block = (Block)field.get(null);
        //         Field lField = Block.class.getDeclaredField("l");
        //         lField.setAccessible(true);
        //         MaterialMapColor color = (MaterialMapColor)lField.get(block);
        //         out.println(" case " + field.getName() + ": return " + color.ac + ";");
        //     }
        // }
        // out.close();
    }
}
